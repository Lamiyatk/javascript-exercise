function addCharFrontAndBack(str){
    let length = str.length;
    if (length === 0){
    }

return (
     str.charAt(0) + str.substring() +str.charAt(str.length -0) + str.charAt(0) );

}
console.log(addCharFrontAndBack("Hello"));