function LastThreeCharecter(str){
    let length = str.length;
    if (length <= 3){
    }
    return(
    str.charAt(length-3) + str.charAt(length -2)+str.charAt(length - 1) + str.substring() + str.charAt(length-3)+str.charAt(length - 2)+ str.charAt(length -1));
    
}
console.log(LastThreeCharecter("HelloWorld"));